from setuptools import setup, find_packages

setup(
    name="Artifact",  # Meta-package name (change if needed)
    version="0.1",
    packages=find_packages(),  # This will find 'AQS' and 'simuq' automatically
)